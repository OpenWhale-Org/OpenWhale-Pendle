# Each package keeps its own node_modules (no workspace hoisting): the gateway
# stages a plugin by copying its directory, so every dependency symlink must
# stay inside it. Links that point outside (core, web3, the sibling package)
# are resolved by the gateway's own ~/.openwhale/node_modules fallbacks.
